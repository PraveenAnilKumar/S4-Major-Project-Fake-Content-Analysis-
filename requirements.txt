# Core ML and data processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Deepfake detection
# For AMD GPU (DirectML) – use tensorflow-cpu==2.10 and tensorflow-directml-plugin
# For NVIDIA GPU, replace with tensorflow (GPU version)
tensorflow-cpu==2.10.0
tensorflow-directml-plugin
opencv-python>=4.8.0
mediapipe>=0.10.7
facenet-pytorch>=2.5.2
pillow>=10.0.0

# Fake news and sentiment analysis (transformers)
torch==2.1.0
torchvision==0.16.0
torchaudio==2.1.0
transformers>=4.30.0,<4.40.0          # compatible with torch 2.1
accelerate>=0.20.0                      # required for Trainer
vaderSentiment>=3.3.2

# Web app and visualisation
streamlit>=1.28.0
plotly>=5.17.0

# Utilities
cachetools>=5.3.0
psutil>=5.9.5
tqdm>=4.66.0
newspaper3k>=0.2.8                      # for URL article extraction
requests>=2.31.0                         # for live news fetching

# Additional indirect dependencies (usually installed automatically, but listed for completeness)
h5py>=3.8.0                               # for saving Keras models
pyyaml>=6.0                                # for Transformers config
